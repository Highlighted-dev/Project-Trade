# How to start server on new machine

You have to install nodemon globally:

```bash
npm install -g nodemon
```

Then you have to install ts-node globally:

```bash
npm install -g ts-node
```
